---
- name: Création namespace
  kubernetes.core.k8s:
    kubeconfig: /etc/rancher/k3s/k3s.yaml
    state: present
    definition:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: "{{ app_name }}"
  become: yes

- name: Check PWD
  bash: pwd
  register: pwd_output
  debug:
    var: pwd_output.stdout

- name: Création volume
  kubernetes.core.k8s:
    kubeconfig: /etc/rancher/k3s/k3s.yaml
    state: present
    src: "./kubernetes/persistent-volume.yml"
    namespace: "{{ app_name }}"
  become: yes

- name: Déploiement API
  kubernetes.core.k8s:
    kubeconfig: /etc/rancher/k3s/k3s.yaml
    state: present
    src: "./kubernetes/api-deployment.yml"
    namespace: "{{ app_name }}"
  become: yes

- name: Déploiement Frontend
  kubernetes.core.k8s:
    kubeconfig: /etc/rancher/k3s/k3s.yaml
    state: present
    src: "./kubernetes/frontend-deployment.yml"
    namespace: "{{ app_name }}"
  become: yes

- name: Attente du déploiement de tous les services
  kubernetes.core.k8s_info:
    kind: Deployment
    name: "{{ item }}"
    namespace: "{{ app_name }}"
    wait: yes
    wait_condition:
      type: Available
      status: "True"
    wait_timeout: 300
  loop:
    - api-deployment
    - frontend-deployment

- name: Vérification status PVC
  kubernetes.core.k8s_info:
    kind: PersistentVolumeClaim
    name: app-data-pvc
    namespace: "{{ app_name }}"
  register: pvc_status
- debug:
    var: pvc_status.resources[0].status.phase